describe('Round', () => {
  it('Should be able to play a round all the way through', () => {
    // a new Round needs players, a shuffle seed, needs to know which player the dealer is, and reference to a card ranker or a deck
    // shuffle the deck
    // deal the cards
    // expect the picker to do the right thing
    // you can either pick or pass
    // so the round object is stateful
    // pickerNotChosenState the picker can either pick or pass
    // trickState
    // endState
  })
})
